window.addEventListener("DOMContentLoaded", () => {
  const cart = document.querySelectorAll(".product-cart img");
  console.log(cart);
});

function addToCart() {
  console.log("hey");
}

// addToCart();

window.addEventListener("resize", function() {
  console.log("Screen size changed!");
});
